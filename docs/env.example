# Copy these names into the external host secret manager; do not commit real values.
PORT=3000
DATABASE_URL=mysql://user:password@host:3306/propwise
JWT_SECRET=replace-with-a-long-random-secret
FRONTEND_URL=http://localhost:5173
BUILT_IN_FORGE_API_URL=https://forge.example.invalid
BUILT_IN_FORGE_API_KEY=replace-with-server-only-key
VITE_FRONTEND_FORGE_API_URL=https://forge.example.invalid
VITE_FRONTEND_FORGE_API_KEY=replace-with-frontend-proxy-key
VITE_APP_TITLE=PropWise AI
# Optional only when bypassing the managed server-side LLM helper:
GEMINI_API_KEY=replace-with-provider-key
