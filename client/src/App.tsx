import { useEffect, useMemo, useState } from "react";
import { Route, Switch, Link, useLocation } from "wouter";
import { startLogin } from "@/const";
import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout from "./components/DashboardLayout";
import { MapView } from "./components/Map";
import { Toaster, toast } from "sonner";
import { trpc } from "./lib/trpc";
import { Button } from "./components/ui/button";
import { Input } from "./components/ui/input";
import { Label } from "./components/ui/label";
import { Card, CardContent, CardHeader, CardTitle } from "./components/ui/card";
import { Badge } from "./components/ui/badge";
import { Progress } from "./components/ui/progress";
import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer } from "recharts";
import { ArrowRight, BarChart3, Bookmark, CheckCircle2, ChevronRight, Compass, FileCheck2, Globe2, Loader2, LogIn, MapPinned, ShieldCheck, Sparkles, TrendingUp, Upload, WandSparkles } from "lucide-react";

const money = (n:number) => new Intl.NumberFormat("en-IN", {style:"currency",currency:"INR",maximumFractionDigits:0}).format(n);
const shortMoney = (n:number) => n>=10000000?`₹${(n/10000000).toFixed(1)}Cr`:n>=100000?`₹${(n/100000).toFixed(1)}L`:`₹${Math.round(n).toLocaleString("en-IN")}`;

function Landing(){ const [,setLocation]=useLocation(); return <div className="propwise-landing-bg min-h-screen overflow-hidden text-slate-900"><header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-6 lg:px-10"><Link href="/" className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-sky-600 text-white"><Sparkles className="h-5 w-5"/></div><div><div className="font-semibold tracking-tight">PropWise AI</div><div className="text-[10px] uppercase tracking-[.2em] text-slate-400">Location intelligence</div></div></Link><div className="hidden items-center gap-8 text-sm text-slate-500 md:flex"><a href="#how">How it works</a><a href="#signals">Signals</a><a href="#extension">Extension</a></div><Button variant="outline" onClick={()=>setLocation("/login")}>Sign in</Button></header><main><section className="mx-auto grid max-w-7xl gap-12 px-6 pb-20 pt-12 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:px-10 lg:pt-20"><div><Badge className="border-sky-200 bg-sky-50 text-sky-700">AI-assisted land intelligence</Badge><h1 className="mt-6 max-w-2xl text-5xl font-semibold leading-[1.06] tracking-[-.045em] text-slate-950 sm:text-6xl">Know the <span className="text-sky-600">place</span> before you buy the property.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-500">Pin a location, add the site details, and get a transparent estimate of value, connectivity, growth signals, and the questions worth asking next.</p><div className="mt-9 flex flex-wrap gap-3"><Button size="lg" className="gap-2" onClick={()=>setLocation("/analyze")}>Analyze a location <ArrowRight className="h-4 w-4"/></Button><Button size="lg" variant="outline" onClick={()=>setLocation("/login")}>Create workspace</Button></div><div className="mt-8 flex flex-wrap gap-x-6 gap-y-2 text-sm text-slate-500"><span className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-emerald-600"/>Private workspace</span><span className="flex items-center gap-2"><FileCheck2 className="h-4 w-4 text-emerald-600"/>Explainable signals</span><span className="flex items-center gap-2"><Globe2 className="h-4 w-4 text-emerald-600"/>Map-first workflow</span></div></div><div className="relative"><div className="absolute -inset-5 rounded-[2.5rem] bg-sky-100/60 blur-2xl"/><Card className="relative overflow-hidden rounded-[2rem] border-0 bg-slate-950 text-white shadow-2xl shadow-sky-200/70"><div className="border-b border-white/10 px-6 py-5"><div className="flex items-center justify-between"><div><div className="text-sm text-white/55">Location snapshot</div><div className="mt-1 text-lg font-medium">Sarjapur Road, Bengaluru</div></div><div className="rounded-full bg-emerald-400/15 px-3 py-1 text-xs font-medium text-emerald-300">Indicative</div></div></div><div className="grid grid-cols-2 gap-px bg-white/10"><div className="bg-slate-950 p-6"><div className="text-xs text-white/45">Estimated value</div><div className="mt-2 text-2xl font-semibold">₹76.5L</div><div className="mt-1 text-xs text-emerald-300">₹6,120 / sq.ft</div></div><div className="bg-slate-950 p-6"><div className="text-xs text-white/45">Location score</div><div className="mt-2 text-2xl font-semibold">86<span className="text-sm text-white/40">/100</span></div><div className="mt-1 text-xs text-sky-300">Suburban · 88% confidence</div></div></div><div className="p-6"><div className="flex items-center justify-between text-xs text-white/50"><span>5-year projection</span><span>Model-based estimate</span></div><div className="mt-4 h-28"><ResponsiveContainer width="100%" height="100%"><AreaChart data={[{x:"Now",v:72},{x:"1Y",v:80},{x:"3Y",v:95},{x:"5Y",v:118}]}><defs><linearGradient id="g" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#38bdf8" stopOpacity=".55"/><stop offset="100%" stopColor="#38bdf8" stopOpacity="0"/></linearGradient></defs><XAxis dataKey="x" hide/><YAxis hide/><Tooltip contentStyle={{background:"#0f172a",border:"1px solid #334155",borderRadius:12,color:"white"}}/><Area type="monotone" dataKey="v" stroke="#38bdf8" fill="url(#g)" strokeWidth={3}/></AreaChart></ResponsiveContainer></div></div></Card></div></section><section id="signals" className="border-y border-[#caa77a]/70 bg-[#d8c2a0]/92 backdrop-blur-sm"><div className="mx-auto grid max-w-7xl gap-5 px-6 py-12 sm:grid-cols-2 lg:grid-cols-4 lg:px-10">{[[MapPinned,"Pin any place","Search, click, and capture coordinates."],[BarChart3,"Read the signals","See value, access, demand, and growth."],[WandSparkles,"Ask better questions","Get cautious AI explanations and due diligence prompts."],[Compass,"Compare options","Bring your shortlist into one decision view."]].map(([Icon,title,body]:any)=><div key={title} className="rounded-2xl bg-[#ead8b8]/85 p-5 shadow-sm"><Icon className="h-5 w-5 text-sky-600"/><h3 className="mt-4 font-medium">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{body}</p></div>)}</div></section><section id="how" className="mx-auto max-w-7xl px-6 py-20 lg:px-10"><div className="max-w-2xl"><div className="text-sm font-medium text-sky-600">A calmer research loop</div><h2 className="mt-3 text-3xl font-semibold tracking-tight">From a map pin to a next-step plan.</h2><p className="mt-4 text-slate-500">PropWise keeps the math visible and the uncertainty explicit. It is an intelligence layer for early research, not a substitute for legal, financial, or technical due diligence.</p></div><div className="mt-10 grid gap-5 md:grid-cols-3">{[["01","Select","Search a neighborhood or pin a location directly on the map."],["02","Analyze","Add area, category, access, and amenity inputs for a tailored estimate."],["03","Decide","Save, compare, and turn the output into a verification checklist."]].map(([n,t,b])=><div key={n} className="rounded-3xl border border-slate-200 p-6"><div className="text-sm font-medium text-sky-600">{n}</div><div className="mt-12 text-xl font-semibold">{t}</div><p className="mt-3 text-sm leading-6 text-slate-500">{b}</p></div>)}</div></section><section id="extension" className="bg-slate-950 px-6 py-16 text-white lg:px-10"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-8 md:flex-row md:items-center"><div><div className="flex items-center gap-2 text-sky-300"><Upload className="h-4 w-4"/>Chrome extension ready</div><h2 className="mt-3 text-3xl font-semibold">Analyze supported listing pages without losing context.</h2><p className="mt-3 max-w-2xl text-white/55">The bundled Manifest V3 extension extracts visible listing details, sends them to your workspace, and links back to the full analysis. It never bypasses site protections.</p></div><Button variant="secondary" className="shrink-0" onClick={()=>setLocation("/login")}>Open workspace <ChevronRight className="ml-1 h-4 w-4"/></Button></div></section></main><footer className="mx-auto flex max-w-7xl justify-between px-6 py-8 text-xs text-slate-400 lg:px-10"><span>PropWise AI · research with context</span><span>Estimates are indicative, not financial advice.</span></footer></div> }

function Login(){ const {user,loading,error}=useAuth(); const [,setLocation]=useLocation(); useEffect(()=>{if(user)setLocation("/dashboard");},[user,setLocation]); return <div className="propwise-login-bg min-h-screen px-6 py-8 text-slate-950"><div className="mx-auto flex max-w-6xl items-center justify-between"><Link href="/" className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-950 text-sky-300 shadow-xl shadow-sky-200"><Sparkles className="h-5 w-5"/></div><div><div className="font-semibold tracking-tight">PropWise AI</div><div className="text-[10px] uppercase tracking-[.2em] text-slate-400">Location intelligence</div></div></Link><Link href="/" className="text-sm text-slate-500 hover:text-slate-950">Back to home</Link></div><main className="mx-auto grid max-w-6xl gap-10 py-16 lg:grid-cols-[.9fr_1.1fr] lg:items-center"><section><div className="text-sm font-semibold uppercase tracking-[.2em] text-sky-600">Private research workspace</div><h1 className="mt-5 max-w-xl text-5xl font-semibold leading-[1.05] tracking-[-.05em]">Sign in to keep every location decision together.</h1><p className="mt-6 max-w-lg text-lg leading-8 text-slate-500">Save exact map pins, compare properties, and return to your analysis history from any device.</p><div className="mt-8 grid gap-3 text-sm text-slate-600"><div className="rounded-2xl border border-[#d2b58a] bg-[#dcc5a0]/80 p-4 shadow-sm"><span className="font-medium text-slate-950">Exact pins.</span> Your selected coordinates become the analysis input.</div><div className="rounded-2xl border border-[#d2b58a] bg-[#dcc5a0]/80 p-4 shadow-sm"><span className="font-medium text-slate-950">Explainable outputs.</span> Review the factors before acting.</div></div></section><Card className="rounded-[2rem] border-[#dbc39f] bg-[#ead8b8]/92 p-2 shadow-2xl shadow-sky-200/60 backdrop-blur"><CardContent className="p-8 sm:p-10"><div className="mx-auto max-w-sm"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-sky-50 text-sky-700"><LogIn className="h-6 w-6"/></div><h2 className="mt-7 text-2xl font-semibold">Welcome back</h2><p className="mt-2 text-sm leading-6 text-slate-500">Use secure sign-in to open your PropWise workspace.</p>{error&&<div className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">Sign-in status could not be loaded. Please try again.</div>}<Button className="mt-8 h-12 w-full gap-2 rounded-xl" onClick={()=>startLogin()} disabled={loading}>{loading?<Loader2 className="h-4 w-4 animate-spin"/>:<LogIn className="h-4 w-4"/>}{loading?"Checking session…":"Continue with secure sign-in"}</Button><p className="mt-5 text-center text-xs leading-5 text-slate-400">By continuing, you agree to use PropWise AI for research support and to verify important property facts independently.</p></div></CardContent></Card></main></div> }

function Header({title,kicker}:{title:string;kicker:string}){return <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="text-xs font-semibold uppercase tracking-[.18em] text-sky-600">{kicker}</div><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">{title}</h1></div><Link href="/analyze"><Button className="gap-2"><MapPinned className="h-4 w-4"/>New analysis</Button></Link></div>}
function Dashboard(){ const {data:props=[]}=trpc.properties.list.useQuery(); const {data:history=[]}=trpc.analysis.history.useQuery(); const avg=props.length?props.reduce((s:any,p:any)=>s+(p.areaSqft||0),0)/props.length:0; return <DashboardLayout><Header kicker="Workspace overview" title="Your property intelligence desk"/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Saved properties" value={String(props.length)} detail="Across your workspace" icon={<Bookmark/>}/><Metric label="Avg. site area" value={avg?`${Math.round(avg).toLocaleString("en-IN")} sq.ft`:"—"} detail="From saved inputs" icon={<Globe2/>}/><Metric label="Analyses run" value={String(history.length)} detail="Your research trail" icon={<BarChart3/>}/><Metric label="Decision mode" value="Indicative" detail="Verify before acting" icon={<ShieldCheck/>}/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center justify-between"><span>Research activity</span><span className="text-xs font-normal text-slate-400">Saved analysis history</span></CardTitle></CardHeader><CardContent><div className="h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={(history.length?history.slice(0,7).reverse():[{createdAt:Date.now(),result:{estimatedValue:0}}]).map((h:any,i:number)=>({name:`${i+1}`,value:h.result?.estimatedValue||0}))}><defs><linearGradient id="dash" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#0284c7" stopOpacity=".3"/><stop offset="100%" stopColor="#0284c7" stopOpacity="0"/></linearGradient></defs><XAxis dataKey="name" stroke="#94a3b8"/><YAxis tickFormatter={v=>shortMoney(v)} stroke="#94a3b8"/><Tooltip formatter={(v:any)=>money(v)} contentStyle={{borderRadius:14,border:"1px solid #e2e8f0"}}/><Area type="monotone" dataKey="value" stroke="#0284c7" fill="url(#dash)" strokeWidth={3}/></AreaChart></ResponsiveContainer></div></CardContent></Card><Card className="rounded-3xl border-0 bg-slate-950 text-white shadow-sm"><CardHeader><CardTitle className="text-white">A useful reminder</CardTitle></CardHeader><CardContent><div className="grid h-full content-center"><CheckCircle2 className="h-8 w-8 text-emerald-300"/><p className="mt-5 text-lg leading-8 text-white/85">A confident score is not a title search. Use the AI notes to decide what to verify next.</p><Link href="/about" className="mt-6 text-sm text-sky-300">Read methodology <ArrowRight className="ml-1 inline h-4 w-4"/></Link></div></CardContent></Card></div></DashboardLayout> }
function Metric({label,value,detail,icon}:{label:string;value:string;detail:string;icon:React.ReactNode}){return <Card className="rounded-3xl border-0 shadow-sm"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="text-sm text-slate-500">{label}</div><div className="rounded-xl bg-sky-50 p-2 text-sky-600">{icon}</div></div><div className="mt-5 text-2xl font-semibold">{value}</div><div className="mt-1 text-xs text-slate-400">{detail}</div></CardContent></Card>}

function Analyze(){ const [,setLocation]=useLocation(); const [coords,setCoords]=useState({latitude:12.9716,longitude:77.5946}); const [hasPin,setHasPin]=useState(false); const [mapObj,setMapObj]=useState<any>(); const [search,setSearch]=useState(""); const [mapError,setMapError]=useState(""); const [mapLoading,setMapLoading]=useState(true); const save=trpc.saved.add.useMutation(); const [form,setForm]=useState<any>({title:"",address:"Bengaluru, Karnataka",areaSqft:1200,propertyType:"Residential plot",category:"residential",roadAccess:"Paved road",amenities:["School"]}); const [result,setResult]=useState<any>(); const mutation=trpc.analysis.analyze.useMutation({onSuccess:r=>{setResult(r);toast.success("Location analyzed");},onError:e=>toast.error(e.message)}); const update=(k:string,v:any)=>setForm((f:any)=>({...f,[k]:v})); const locate=()=>{if(!mapObj||!search.trim())return;setMapError("");new google.maps.Geocoder().geocode({address:search},(res:any,status:any)=>{if(status!=="OK"||!res?.[0]){setMapError("Location could not be found. Try a more specific address.");return;}const p=res[0].geometry.location;const next={latitude:p.lat(),longitude:p.lng()};setCoords(next);setHasPin(false);update("address",res[0].formatted_address||search);mapObj.setCenter(p);mapObj.setZoom(15);new google.maps.Marker({map:mapObj,position:p,title:"Search result — click the exact site to confirm"});});}; const analyze=()=>mutation.mutate({...form,latitude:coords.latitude,longitude:coords.longitude,areaSqft:Number(form.areaSqft),title:form.title||"Untitled Bengaluru property",amenities:form.amenities||[]}); const keep=()=>{if(result?.propertyId){save.mutate({propertyId:result.propertyId,tags:[result.areaType]}, {onSuccess:()=>{toast.success("Property saved to your shortlist");setLocation("/saved")},onError:e=>toast.error(e.message)});}}; return <DashboardLayout><Header kicker="Map intelligence" title="Analyze a location"/><div className="grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><Card className="overflow-hidden rounded-3xl border-0 shadow-sm"><div className="relative h-[430px] bg-slate-100"><MapView className="h-full" initialCenter={{lat:coords.latitude,lng:coords.longitude}} initialZoom={12} onMapReady={(map:any)=>{setMapObj(map);setMapLoading(false);map.addListener("click",(e:any)=>{const next={latitude:e.latLng.lat(),longitude:e.latLng.lng()};setCoords(next);setHasPin(true);new google.maps.Marker({map,position:{lat:next.latitude,lng:next.longitude},title:"Exact price pin"});new google.maps.Geocoder().geocode({location:e.latLng},(res:any)=>{if(res?.[0]){update("address",res[0].formatted_address);}});});}}/><div className="absolute left-4 right-4 top-4 z-10 flex gap-2"><Input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")locate()}} placeholder="Search a place or address" className="bg-white/95 shadow-lg"/><Button variant="secondary" onClick={locate}>Search</Button></div>{mapLoading&&<div className="absolute inset-0 grid place-items-center bg-slate-100/80 text-sm text-slate-500">Loading map…</div>}{mapError&&<div className="absolute bottom-4 left-4 right-4 rounded-xl bg-red-50 p-3 text-xs text-red-700">{mapError}</div>}<div className="absolute left-4 top-20 rounded-2xl bg-white/95 p-3 shadow-lg backdrop-blur"><div className="text-xs font-semibold uppercase tracking-wider text-slate-400">{hasPin?"Exact price pin selected":"Pin required for price"}</div><div className="mt-1 font-mono text-sm text-slate-800">{coords.latitude.toFixed(5)}, {coords.longitude.toFixed(5)}</div></div></div><div className="flex flex-wrap items-center justify-between gap-4 border-t border-slate-100 p-5"><div><div className="text-sm font-medium">Pin the site on the map</div><div className="mt-1 text-xs text-slate-400">Search only centers the map. Click the exact site to confirm the authoritative price pin.</div></div><Button variant="outline" onClick={()=>{setCoords({latitude:12.9716,longitude:77.5946});setHasPin(false);}}>Reset view</Button></div></Card><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle>Site details</CardTitle><p className="text-sm text-slate-500">Add only what you know. Unavailable fields stay optional.</p></CardHeader><CardContent className="space-y-4"><Field label="Property label"><Input value={form.title} onChange={e=>update("title",e.target.value)} placeholder="e.g. Sarjapur corner plot"/></Field><Field label="Location / address"><Input value={form.address} onChange={e=>update("address",e.target.value)}/></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Site area (sq.ft)"><Input type="number" value={form.areaSqft} onChange={e=>update("areaSqft",e.target.value)}/></Field><Field label="Property type"><select className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm" value={form.propertyType} onChange={e=>update("propertyType",e.target.value)}><option>Residential plot</option><option>Apartment</option><option>Commercial site</option><option>Agricultural land</option></select></Field></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Category"><select className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm" value={form.category} onChange={e=>update("category",e.target.value)}><option value="residential">Residential</option><option value="commercial">Commercial</option><option value="agricultural">Agricultural</option></select></Field><Field label="Road access"><select className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm" value={form.roadAccess} onChange={e=>update("roadAccess",e.target.value)}><option>Main road</option><option>Paved road</option><option>Narrow road</option><option>Unpaved</option><option>Unknown</option></select></Field></div><Field label="Known nearby amenities"><div className="flex flex-wrap gap-2">{["School","Hospital","Transit","Retail","Bank"].map(a=><button type="button" key={a} onClick={()=>update("amenities",(form.amenities||[]).includes(a)?form.amenities.filter((x:string)=>x!==a):[...(form.amenities||[]),a])} className={`rounded-full border px-3 py-1.5 text-xs ${form.amenities?.includes(a)?"border-sky-200 bg-sky-50 text-sky-700":"border-slate-200 text-slate-500"}`}>{a}</button>)}</div></Field><Button className="mt-2 w-full gap-2" size="lg" onClick={analyze} disabled={mutation.isPending||!hasPin}>{mutation.isPending?"Calculating exact location…":hasPin?"Calculate price from this pin":"Pin the exact place to calculate price"}<ArrowRight className="h-4 w-4"/></Button><p className="text-center text-[11px] leading-5 text-slate-400">Estimates are model predictions and should not be treated as guaranteed market values or financial advice.</p></CardContent></Card></div>{result&&<ResultPanel result={result} onSave={keep}/>}</DashboardLayout> }
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><Label className="mb-2 block text-xs font-medium text-slate-500">{label}</Label>{children}</div>}
function ResultPanel({result,onSave}:{result:any;onSave:()=>void}){return <div className="mt-6 space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Estimated value" value={shortMoney(result.estimatedValue)} detail={`Indicative range ${shortMoney(result.marketRange[0])} – ${shortMoney(result.marketRange[1])}`} icon={<TrendingUp/>}/><Metric label="Price / sq.ft" value={money(result.pricePerSqft)} detail="Model prediction" icon={<BarChart3/>}/><Metric label="Location score" value={`${result.locationScore}/100`} detail={`${result.areaType} · ${result.classificationConfidence}% confidence`} icon={<Compass/>}/><Metric label="Investment score" value={`${result.investmentScore}/100`} detail={`Indicative ${result.annualAppreciation}% annual growth`} icon={<Sparkles/>}/></div><div className="grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle>Transparent scorecard</CardTitle></CardHeader><CardContent className="space-y-4">{result.factors.map((f:any)=><div key={f.name}><div className="mb-2 flex justify-between text-sm"><span>{f.name} <span className="text-xs text-slate-400">· {f.weight}%</span></span><span className="font-medium">{f.score}</span></div><Progress value={f.score}/><div className="mt-1 text-xs text-slate-400">{f.note}</div></div>)}</CardContent></Card><Card className="rounded-3xl border-0 bg-slate-950 text-white shadow-sm"><CardHeader><CardTitle className="text-white">AI research brief</CardTitle></CardHeader><CardContent><p className="leading-7 text-white/75">{result.aiExplanation?.summary}</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><div className="text-xs uppercase tracking-wider text-emerald-300">Signals in favor</div><ul className="mt-2 space-y-2 text-sm text-white/70">{result.aiExplanation?.advantages?.map((x:string)=><li key={x}>+ {x}</li>)}</ul></div><div><div className="text-xs uppercase tracking-wider text-amber-300">Check next</div><ul className="mt-2 space-y-2 text-sm text-white/70">{result.aiExplanation?.concerns?.map((x:string)=><li key={x}>! {x}</li>)}</ul></div></div><div className="mt-6 border-t border-white/10 pt-4 text-xs leading-5 text-white/45">{result.methodology}</div></CardContent></Card></div><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle>Future value projection</CardTitle><p className="text-sm text-slate-500">Indicative projection, not a guarantee of appreciation.</p></CardHeader><CardContent><div className="grid gap-4 sm:grid-cols-3">{result.futureValues.map((v:any)=><div key={v.horizon} className="rounded-2xl bg-slate-50 p-4"><div className="text-xs text-slate-400">{v.horizon}</div><div className="mt-2 text-xl font-semibold">{shortMoney(v.value)}</div></div>)}</div><div className="mt-5 flex flex-wrap gap-3"><Button onClick={onSave} className="gap-2"><Bookmark className="h-4 w-4"/>Keep in history</Button><Link href="/saved"><Button variant="outline">View saved properties</Button></Link></div></CardContent></Card></div>}

function Saved(){const {data=[],isLoading}=trpc.saved.list.useQuery(); const remove=trpc.saved.remove.useMutation({onSuccess:()=>toast.success("Removed from saved")}); return <DashboardLayout><Header kicker="Your shortlist" title="Saved properties"/><Card className="rounded-3xl border-0 shadow-sm"><CardContent className="p-0">{isLoading?<div className="p-8 text-sm text-slate-500">Loading saved properties…</div>:!data.length?<div className="grid place-items-center px-6 py-20 text-center"><Bookmark className="h-8 w-8 text-slate-300"/><h3 className="mt-4 font-medium">Your shortlist is empty</h3><p className="mt-2 max-w-sm text-sm text-slate-500">Run an analysis first, then save the locations that deserve a closer look.</p><Link href="/analyze" className="mt-5"><Button>Start an analysis</Button></Link></div>:<div className="divide-y divide-slate-100">{data.map((item:any)=><div key={item.id} className="flex flex-col justify-between gap-4 p-6 sm:flex-row sm:items-center"><div><div className="font-medium">{item.property?.title||"Saved property"}</div><div className="mt-1 text-sm text-slate-500">{item.property?.category} · {item.property?.areaSqft?.toLocaleString("en-IN")} sq.ft</div></div><div className="flex items-center gap-3"><Badge variant="secondary">Needs analysis review</Badge><Button variant="ghost" size="sm" onClick={()=>remove.mutate({id:item.id})}>Remove</Button></div></div>)}</div>}</CardContent></Card></DashboardLayout>}
function Compare(){const {data=[]}=trpc.properties.list.useQuery(); const [selected,setSelected]=useState<number[]>([]); const compare=trpc.comparison.analyze.useMutation({onSuccess:r=>toast.success(r.recommendation)}); return <DashboardLayout><Header kicker="Decision support" title="Compare your shortlist"/><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle>Select 2–4 properties</CardTitle><p className="text-sm text-slate-500">Recommendations are score-based and never override due diligence.</p></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-2">{data.map((p:any)=><button key={p.id} onClick={()=>setSelected(s=>s.includes(p.id)?s.filter(x=>x!==p.id):s.length<4?[...s,p.id]:s)} className={`rounded-2xl border p-4 text-left transition ${selected.includes(p.id)?"border-sky-300 bg-sky-50":"border-slate-200 hover:border-sky-200"}`}><div className="flex items-center justify-between"><span className="font-medium">{p.title}</span>{selected.includes(p.id)&&<CheckCircle2 className="h-5 w-5 text-sky-600"/>}</div><div className="mt-2 text-sm text-slate-500">{p.propertyType} · {p.areaSqft?.toLocaleString("en-IN")} sq.ft</div></button>)}</div><Button className="mt-6" disabled={selected.length<2||compare.isPending} onClick={()=>compare.mutate({propertyIds:selected})}>Compare selected properties</Button>{compare.data&&<div className="mt-6 rounded-2xl bg-slate-950 p-5 text-sm leading-7 text-white/75">{compare.data.recommendation}</div>}</CardContent></Card></DashboardLayout>}
function History(){const {data=[],isLoading}=trpc.analysis.history.useQuery(); return <DashboardLayout><Header kicker="Research trail" title="Analysis history"/><Card className="rounded-3xl border-0 shadow-sm"><CardContent className="p-0">{isLoading?<div className="p-8 text-sm text-slate-500">Loading history…</div>:!data.length?<div className="p-10 text-center text-sm text-slate-500">No analyses yet. Your next location study will appear here.</div>:<div className="divide-y divide-slate-100">{data.map((a:any)=><div key={a.id} className="flex flex-col justify-between gap-3 p-6 sm:flex-row sm:items-center"><div><div className="font-medium">Analysis #{a.id}</div><div className="mt-1 text-sm text-slate-500">{new Date(a.createdAt).toLocaleString()}</div></div><div className="flex items-center gap-4"><div className="text-right"><div className="font-semibold">{shortMoney(a.result?.estimatedValue||0)}</div><div className="text-xs text-slate-400">{a.result?.areaType} · {a.result?.locationScore}/100</div></div><ChevronRight className="h-4 w-4 text-slate-300"/></div></div>)}</div>}</CardContent></Card></DashboardLayout>}
function Profile(){const {user}=useAuth(); return <DashboardLayout><Header kicker="Workspace" title="Profile"/><Card className="max-w-2xl rounded-3xl border-0 shadow-sm"><CardContent className="space-y-5 p-6"><div><div className="text-xs uppercase tracking-wider text-slate-400">Name</div><div className="mt-1 text-lg font-medium">{user?.name||"PropWise user"}</div></div><div><div className="text-xs uppercase tracking-wider text-slate-400">Email</div><div className="mt-1 text-lg font-medium">{user?.email||"Managed by secure sign-in"}</div></div><div className="rounded-2xl bg-slate-50 p-4 text-sm leading-6 text-slate-500">Your workspace data is scoped to your authenticated account. Use the profile area to confirm your identity before saving sensitive research notes.</div></CardContent></Card></DashboardLayout>}
function About(){return <DashboardLayout><Header kicker="Methodology" title="About PropWise AI"/><div className="grid max-w-5xl gap-6 md:grid-cols-2"><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle>What the model does</CardTitle></CardHeader><CardContent className="space-y-4 text-sm leading-7 text-slate-500"><p>PropWise combines the coordinates, site area, category, road-access signal, and user-supplied amenities into an indicative scorecard.</p><p>The valuation engine is intentionally transparent and JavaScript-native. It is a development model until it is calibrated against a licensed, representative dataset.</p></CardContent></Card><Card className="rounded-3xl border-0 shadow-sm"><CardHeader><CardTitle>What it does not do</CardTitle></CardHeader><CardContent className="space-y-4 text-sm leading-7 text-slate-500"><p>It does not verify title, ownership, approvals, taxes, zoning, utilities, legal status, or live market comparables.</p><p>Future appreciation and investment scores are uncertain, model-based projections and are not financial advice.</p></CardContent></Card></div></DashboardLayout>}
function App(){return <><Toaster/><Switch><Route path="/" component={Landing}/><Route path="/login" component={Login}/><Route path="/dashboard" component={Dashboard}/><Route path="/analyze" component={Analyze}/><Route path="/saved" component={Saved}/><Route path="/compare" component={Compare}/><Route path="/history" component={History}/><Route path="/profile" component={Profile}/><Route path="/about" component={About}/><Route><Landing/></Route></Switch></>}
export default App;
